# `staticfiles` content

This is a static content storage accessible via HTTP.

Any file added to the *staticfiles* directory will appear here.

Note that *static* directory content is excluded from the repo.
You need to adjust **.gitignore file** to change this behavior.
